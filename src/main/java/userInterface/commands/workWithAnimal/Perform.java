package userInterface.commands.workWithAnimal;

import userInterface.Menu;
import userInterface.commands.MenuCommand;

public class Perform extends MenuCommand {
    public Perform(Menu menu) {
        super(menu, "Perform a command");
    }
    public void run(){
//        super.getView().
    }
}
